services:
  quark-sync:
    image: hsiangleev/quark-sync:latest
    container_name: quark-sync
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
      - ./downloads:/app/downloads
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
