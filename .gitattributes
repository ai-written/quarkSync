# Shell 脚本必须保持 LF。
# 本仓库若在 Windows 上以 core.autocrlf=true 检出，脚本会被转成 CRLF，
# 而 Docker COPY 原样保留字节，导致 shebang 变成 "#!/bin/sh\r"，
# Alpine 下 ENTRYPOINT 直接 exec 失败（报 no such file or directory）。
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
